"""Standalone ingestion-service smoke app.

Durable ingestion work runs in ``apps/temporal-worker``. This package exists
so the workspace member has a non-conflicting import path and a health surface.
"""
