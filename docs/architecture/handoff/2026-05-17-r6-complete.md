# Session handoff — 2026-05-17: R6 complete; remediation stream done

> Purpose: capture exact state so the next session can resume without
> re-deriving context. References the
> [remediation plan](../REMEDIATION_PLAN.md), the
> [R5 handoff](2026-05-17-r5-complete.md),
> [R4 handoff](2026-05-17-r4-partial.md),
> [R3 handoff](2026-05-17-r3-partial.md),
> [R2 handoff](2026-05-17-r2-complete.md), and
> [R1 handoff](2026-05-16-r1-complete.md).

## What shipped this session

**Phase R6 — Polish** — all three slices. The remediation stream
(R1–R6) is now **complete** except for R4.S6 (the live-cluster
benchmark, documented in the R4 handoff and ADR-0031 § "Deferred").

| Slice | Outcome |
|---|---|
| **R6.S1** README typo | Line 1 was `de# SentinelRAG`. Fixed to `# SentinelRAG`. (Caught by the original 2026-05-16 review checklist.) |
| **R6.S2** Quick-start audit | Read the README quick-start block end to end and checked it against current code. Two real drift items patched: (a) frontend step said `cd apps/frontend && npm install && npm run dev` but the workspace is a pnpm one and the Makefile already exposes `make frontend` that runs `pnpm dev` — README now points at `make install` + `make frontend`; (b) the README's "30 ADRs" claim is stale (we're at 37 after R4.S7 + R5 added 7 — 0031 supersession + 0032 RTBF + 0033 migrations + 0034 prompt-injection + 0035 PII + 0036 sharding + 0037 streaming). The `curl` example and the `Bearer dev` token guidance were verified correct. **Live `make up` smoke remains Docker-blocked** on this Windows host — same constraint that's pinned every R-phase's integration suite. |
| **R6.S3** Trailing follow-ups | Landed the one cheap follow-up from the R4 handoff: a startup guard at `apps/api/app/lifecycle.py::_build_retrieval_client` that fails loud when `RETRIEVAL_TRANSPORT=http` but `RETRIEVAL_SERVICE_TOKEN` is empty. Without the guard, the API pod would boot and every `/query` would 503 against the retrieval-service. With it, the API process exits at lifespan startup with an actionable error. 4 new unit tests cover the four branches (in-process, http+token, http+no-token, unknown transport). The remaining follow-ups from R1–R5 handoffs are each their own implementation PR (each sits under one of the six R5 ADRs), not R6 polish. |

## Verification gates passed locally

```bash
uv run ruff check apps packages                  # → 0 errors
uv run pytest -m unit                            # → 250 passed (R5 baseline 246 + 4 new lifecycle guard tests)
helm template infra/helm/sentinelrag             # → clean
uv run python -c "from app.main import create_app; create_app()"  # → 45 routes
```

Integration suite stayed deferred for the same Docker named-pipe
reason as R1–R5 — no new R6 case actually exercises Postgres/Redis,
so this is a non-event for R6 specifically.

## State of the codebase

### Modified files
- `README.md` — typo fix on line 1; ADR-count refresh (30 → 37); frontend quick-start swapped from `npm` to `make install` + `make frontend`.
- `apps/api/app/lifecycle.py` — `_build_retrieval_client` raises a clear `RuntimeError` at startup when `RETRIEVAL_TRANSPORT=http` + empty `RETRIEVAL_SERVICE_TOKEN`.
- `docs/architecture/REMEDIATION_PLAN.md` — R6 flipped to 🟢; current-phase block says "done except R4.S6 (live cluster)."

### New files
- `apps/api/tests/unit/test_lifecycle_retrieval_guard.py` — 4 tests covering the new startup guard branches.
- `docs/architecture/handoff/2026-05-17-r6-complete.md` — this doc.

## Remediation stream — final status

| Phase | Status |
|---|---|
| R1 — Orchestrator surgery + pillar honesty | 🟢 |
| R2 — Layered hallucination cascade | 🟢 |
| R3 — Cost + resilience hardening | 🟢 (S1, S2, S3, S4, S5, S6, S7 all shipped — S6 landed in the R5 session as a piggy-back) |
| R4 — Retrieval-service extraction | 🟡 6/7 — only **S6 benchmark** outstanding, requires a live cluster |
| R5 — ADR backlog catch-up | 🟢 (six ADRs: 0032–0037) |
| R6 — Polish | 🟢 |

**Only blocking item:** R4.S6 benchmark, which requires k6 against a
real cluster running both `RETRIEVAL_TRANSPORT=in-process` and `=http`
in succession. ADR-0029 forbids hand-edited eval numbers, so the
`tests/performance/evals/compare.py` harness has to generate
`docs/operations/eval-report.md` — that's the gate for flipping the
default transport from `in-process` to `http`.

## What's next (outside R-stream)

The R-stream is done. The next backlog items live outside the
remediation plan:

1. **PHASE_PLAN.md work** — the original deploy-readiness items that
   were always independent of the R-stream:
   - First `terraform apply` against a real AWS / GCP account
   - Real-traffic eval/cost numbers in
     `docs/operations/{eval,cost}-report.md` (harness-generated)
   - Drill-recorded RTOs in `docs/operations/dr-drill-report.md`
   - 5-minute demo video
   These ship under the existing PHASE_PLAN.md tracking, not
   REMEDIATION_PLAN.md.
2. **Implementation PRs spawned by R5 ADRs** — see the table at the
   end of the R5 handoff. Each of 0032–0037 sketches a follow-up
   implementation; none of that code is in the repo yet.
3. **R4.S6 benchmark** — fold into the first live-deploy session;
   the cluster is up at that point and k6 already lives at
   `tests/performance/k6/`.

## How to resume

```bash
cd C:\Users\anand\Downloads\Projects\AI\AI_Portfolio\EnterpriseRAG
git status --short              # confirm clean working tree (or PR commits in place)
uv run pytest -m unit           # baseline expect 250 passed
uv run ruff check apps packages # expect clean
```

Then read, in this order (if continuing R-stream-adjacent work):
1. [`docs/architecture/PHASE_PLAN.md`](../PHASE_PLAN.md) — the *other*
   tracker, for the deploy-readiness work the R-stream was parallel to.
2. [`docs/architecture/REMEDIATION_PLAN.md`](../REMEDIATION_PLAN.md) —
   R-stream is closed; only S4.S6 remains, gated on a live cluster.
3. This handoff doc.
4. The six new R5 ADRs (0032–0037) if a follow-up implementation
   session needs scope.

## Risks the next session should pay attention to

1. **The startup guard added in R6.S3 will fail the API pod's boot**
   if `RETRIEVAL_TRANSPORT=http` is set in `values.yaml` (which it is
   in the Helm chart's default — per ADR-0031, deployed environments
   exercise the extracted path) AND `RETRIEVAL_SERVICE_TOKEN` is not
   seeded via ExternalSecret. This is intentional — better to fail at
   boot than 503 every query. The first live-deploy session must
   verify the ESO `secretStoreRef` actually populates the secret
   before the API pod starts.
2. **The README still says "all 10 phases code-side complete"** —
   that's the PHASE_PLAN.md framing and is correct. The R-stream is
   a separate post-review remediation that's also now done; the
   README doesn't surface that distinction by design (it's internal
   architecture-doc concern, not recruiter-facing).
3. **The R-stream's ADRs from R5 have no implementation code yet.**
   A reviewer reading "we do RTBF" must understand ADR-0032 is the
   *decision*, not the *implementation*. The implementation PRs
   queue is the table at the end of the R5 handoff.
