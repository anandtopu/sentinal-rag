# Session handoff — 2026-05-16: R1 complete, R2 next

> Purpose: capture exact state so the next session can resume without
> re-deriving context. References the
> [architecture review](../reviews/2026-05-16-architect-review.md) and
> the [remediation plan](../REMEDIATION_PLAN.md).

## What shipped this session

**Phase R1 — Orchestrator surgery + pillar honesty** — all 5 slices complete.
Closes review findings #1, #2, #6, #7.

| Slice | Outcome |
|---|---|
| **R1.S1** Extract stages package | 16 new files under `apps/api/app/services/rag/` totalling 1186 LOC. Old 883-LOC `rag_orchestrator.py` god method replaced by a 184-LOC `Orchestrator` coordinator + 10 stages averaging 50 LOC each. `RetrievalClient` Protocol + `InProcessRetrievalClient` in place so R4 plugs an HTTP impl behind an existing seam. |
| **R1.S2** Replace raw SQL with repositories | 5 new repositories: `QuerySessionRepository`, `RetrievalResultRepository`, `GeneratedAnswerRepository`, `AnswerCitationRepository`, `UsageRecordRepository`. Stage files contain zero raw SQL. |
| **R1.S3** error_message column | Hand-written migration `migrations/versions/0015_query_session_error_message.py` adds `query_sessions.error_message TEXT`. `normalized_query` poison-pill writes removed from `SessionStage.close`. |
| **R1.S4** Audit secondary-failure isolation | `DualWriteAuditService._safe_write` broadened from `except AuditSinkError` to `except Exception`. New OTel counter `sentinelrag_audit_secondary_failures_total{sink}` with helper `record_audit_secondary_failure`. New unit test covers an unexpected-exception sink. |
| **R1.S5** Delete shim + flip imports | Old `apps/api/app/services/rag_orchestrator.py` deleted. Import sites flipped to `from app.services.rag import ...` in: `apps/api/app/api/v1/routes/query.py`, `apps/temporal-worker/sentinelrag_worker/activities/evaluation.py`, `apps/api/tests/unit/test_query_response_options.py`, `apps/api/tests/unit/test_evaluation_regressions.py`. |

## Verification gates passed locally

```bash
uv run ruff check apps packages                # → 0 errors
uv run pytest -m unit                          # → 194 passed, 0 failures
uv run pyright apps/api/app/services/rag       # → 0 errors on new modules
                                               #   (warnings are pre-existing
                                               #   reportMissingTypeStubs only)
```

Sanity checks:
```bash
# Migration parses + has correct revision/down_revision shape
uv run python -c "import importlib.util; \
  s=importlib.util.spec_from_file_location('m','migrations/versions/0015_query_session_error_message.py'); \
  m=importlib.util.module_from_spec(s); s.loader.exec_module(m); \
  assert m.revision=='0015' and m.down_revision=='0014'"

# App + worker import resolve
uv run python -c "from app.main import create_app; create_app()"
uv run python -c "from sentinelrag_worker.activities import evaluation"
```

## Verification gates deferred (not regressions — environment-blocked)

- **Integration suite** (`uv run pytest -m integration`) — Docker Desktop
  named-pipe permissions (`//./pipe/docker_engine` access denied) blocked
  on this Windows host. Same constraint flagged in `PROGRESS.md` for
  pre-existing integration coverage. **Must run before R1 ships in a real
  PR** to verify:
  - RLS context (`SET LOCAL app.current_tenant_id`) is still set on the
    sessions used by the new repositories.
  - The new `error_message` column round-trips correctly (apply migration
    0015 → fail a query → assert column populated).
  - The new repositories preserve the tenant scoping the raw-SQL inserts
    relied on implicitly.
  - Audit dual-write isolation behaves under a real S3 / MinIO failure
    (the unit test uses a `_BoomSink`; integration should use a real
    misconfigured boto3).
- **Helm / Terraform** — no infra surface changed in R1, so
  `helm template` and `terraform validate` are not on the verification
  path for this phase.

## State of the codebase

### New files
```
apps/api/app/services/rag/
  __init__.py
  orchestrator.py                  # thin coordinator
  types.py                         # QueryContext + configs + result types
  client.py                        # RetrievalClient Protocol + InProcessRetrievalClient
  _helpers.py                      # pure helpers (token overlap, approx_token_count, etc.)
  stages/__init__.py
  stages/session.py                # open + close query_sessions row
  stages/retrieval.py              # call RetrievalClient + persist retrieval_results
  stages/rerank.py                 # bge / no-op rerank + persist
  stages/context_assembly.py       # build context_text + citation map (pure)
  stages/prompt.py                 # resolve prompt version via PromptService
  stages/budget.py                 # cost gate, emits budget.* audit events
  stages/generation.py             # LiteLLM completion call
  stages/grounding.py              # token-overlap layer (R1); R2 stacks NLI + judge
  stages/persistence.py            # generated_answer + citations + usage_records
  stages/audit.py                  # query.executed / query.failed events

apps/api/app/db/repositories/
  query_sessions.py
  retrieval_results.py
  generated_answers.py
  answer_citations.py
  usage_records.py

migrations/versions/
  0015_query_session_error_message.py

apps/api/tests/unit/
  test_rag_stages.py               # 11 new tests covering stages + helpers + QueryContext
```

### Modified files
- `apps/api/app/services/rag_orchestrator.py` — **DELETED** (shim window closed)
- `apps/api/app/db/repositories/__init__.py` — re-exports the 5 new repositories
- `apps/api/app/api/v1/routes/query.py` — `RagOrchestrator` → `Orchestrator`
- `apps/temporal-worker/sentinelrag_worker/activities/evaluation.py` — same
- `apps/api/tests/unit/test_query_response_options.py` — import + monkeypatch target updated
- `apps/api/tests/unit/test_evaluation_regressions.py` — exercises `RerankStage._rerank` directly instead of via the shim
- `apps/api/tests/unit/test_audit_dual_write.py` — new test for unexpected-exception isolation
- `packages/shared/python/sentinelrag_shared/audit/service.py` — broadened catch + meter wire-in
- `packages/shared/python/sentinelrag_shared/telemetry/meters.py` — new `audit_secondary_failures_total` counter + `record_audit_secondary_failure` helper
- `packages/shared/python/sentinelrag_shared/telemetry/__init__.py` — re-exports the new helper

## Open follow-ups surfaced during R1

- **Trace `_build_trace` still uses raw SQL** in `apps/api/app/api/v1/routes/query.py`. Not in R1 scope (R1.S2 covered orchestrator writes only) but a natural next cleanup. Could pair with R2's frontend trace panel work or get its own slice in R6.
- **Frontend trace viewer** doesn't surface `error_message` yet. With R1.S3 the column exists; R2's frontend trace-panel slice should also surface this field.
- **The `_approx_token_count` (`len(text)/4`) helper** is still in use by `BudgetStage`. R3.S3 swaps it for `litellm.token_counter`.

## What's next

### Immediate — R2: Layered hallucination cascade
Closes review finding #4. Plan in
[`REMEDIATION_PLAN.md` § Phase R2](../REMEDIATION_PLAN.md). Estimated 1
session. Decisions already locked:
- **R2.S2 default flag values:** `nli.enabled=on`, `judge.enabled=off`,
  `judge.sample_rate=0.0`. Unit test must assert the trio.
- **Implementation seam:** `apps/api/app/services/rag/stages/grounding.py`
  (currently 15 LOC, layer-1 only). The existing NLI + LLM-judge
  evaluators live in `packages/shared/python/sentinelrag_shared/evaluation/`
  — reuse, don't reimplement.

**First steps for the next session:**
1. Read `packages/shared/python/sentinelrag_shared/evaluation/` (evaluators
   + their public shapes) and `packages/shared/python/sentinelrag_shared/feature_flags/`
   (Unleash adapter — confirm the API the BudgetStage / new GroundingStage will use).
2. Write Alembic 0016 adding `generated_answers.nli_verdict TEXT NULL`
   and `generated_answers.judge_verdict TEXT NULL`.
3. Extend `GeneratedAnswerRepository.create` to take the two new columns.
4. Rewrite `stages/grounding.py` to the 3-layer cascade (overlap →
   NLI when enabled and overlap below threshold → judge when sampled in).
5. Wire Unleash flags + the asserted-default-trio unit test.
6. Update ADR-0010 with an "Implementation notes (2026-MM-DD)" section
   recording the flag scheme + thresholds (do **not** edit the decision
   text — ADR immutability).
7. Frontend trace panel for verdicts.

### Parallelizable — R3: Cost + resilience hardening
Independent of R2 once R1 has landed. The two phases touch different
stage files; can be done by a second contributor or after R2 by the same
one.

### Decision-pending — R4: Retrieval-service extraction
Direction locked (Option A — extract for real, see Decisions log). R1.S1
shipped the `RetrievalClient` Protocol + `InProcessRetrievalClient` so
R4 is wiring the HTTP impl behind the seam, not a second refactor.

## How to resume

```bash
cd C:\Users\anand\Downloads\Projects\AI\AI_Portfolio\EnterpriseRAG
git status --short              # confirm clean working tree (or PR commits in place)
uv run pytest -m unit           # baseline expect 194 passed
uv run ruff check apps packages # expect clean
```

Then read, in this order:
1. [`docs/architecture/PHASE_PLAN.md`](../PHASE_PLAN.md) — overall phase status (note: this file does NOT track Phase R work; the remediation phases live in REMEDIATION_PLAN.md).
2. [`docs/architecture/REMEDIATION_PLAN.md`](../REMEDIATION_PLAN.md) — Phase R ledger.
3. This handoff doc.
4. [`docs/architecture/reviews/2026-05-16-architect-review.md`](../reviews/2026-05-16-architect-review.md) — original findings + the redesign sketch R1 followed.

Then check `TaskList` to see the remediation task tree (R1.S1–S5 already
marked completed; R2–R6 pending and unblocked).

## Risks the next session should pay attention to

1. **Integration suite has not run for R1.** When a Docker-capable host
   becomes available, run `uv run pytest -m integration` and verify
   particularly the RBAC retrieval test, the RLS isolation test, and the
   audit dual-write integration test against MinIO. If any fail, the
   regression is almost certainly in `app/db/repositories/{query_sessions,
   retrieval_results, generated_answers, answer_citations,
   usage_records}.py` or `app/services/rag/stages/{session, retrieval,
   rerank, persistence}.py`.
2. **The R1.S5 shim deletion** removed the `RagOrchestrator` symbol. If
   you find a tool / script / IDE bookmark referring to it, update to
   `Orchestrator` from `app.services.rag`. The grep `RagOrchestrator`
   should return only documentation/historical references.
3. **The `0015` migration is unapplied.** Any environment running R1 code
   against an un-migrated DB will fail on `SessionStage.close` because
   the repository writes `error_message=:err`. Apply migration before
   restarting the API service.
