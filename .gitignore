# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover
.hypothesis/
.mypy_cache/
.pyre/
.pytype/

# uv / virtualenv
.uv-cache/
.venv/
venv/
.python-version
uv.lock.local

# --- Node / Next.js ---
node_modules/
.next/
out/
dist/
.turbo/
*.tsbuildinfo
.npm/
.pnp.*
.yarn/cache
.yarn/unplugged
.yarn/build-state.log
.yarn/install-state.gz
.pnpm-store/

# --- IDEs ---
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# --- Claude Code local settings ---
.claude/

# --- Env / secrets ---
.env
.env.*
!.env.example
.env.local
.env.*.local
.env.development
.env.production
*.pem
*.key
*.crt
secrets.yaml

# --- Build artifacts ---
build/
*.tar.gz
*.zip

# --- Terraform ---
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# --- Synthetic data artifacts (regenerable) ---
scripts/cost/synthetic-month.csv

# --- Helm dependency artifacts ---
# Chart.lock pins versions and IS committed; the extracted dep dirs and
# tarballs are fetched fresh by `helm dependency build` (run in CI).
infra/helm/*/charts/*/
infra/helm/*/charts/*.tgz

# --- Docker ---
.docker/

# --- Logs ---
*.log
logs/

# --- Temporary ---
tmp/
temp/
.cache/

# --- ML model artifacts (downloaded at runtime, not committed) ---
# Match only top-level models directory; ORM models in apps/api/app/db/models/ are committed
/models/
*.bin
*.gguf
*.safetensors
*.onnx

# --- Local development ---
.local/
docker-volumes/

#---- claude --
CLAUDE.md
# --- OpenAPI generated client ---
packages/sentinelrag-sdk/typescript/src/generated/
