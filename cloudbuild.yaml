steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/sentinelrag/sentinel-registry/sentinelrag-api:latest'
      - '-f'
      - 'apps/api/Dockerfile'
      - '.'
images:
  - 'us-central1-docker.pkg.dev/sentinelrag/sentinel-registry/sentinelrag-api:latest'
